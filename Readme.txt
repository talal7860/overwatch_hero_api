- Make sure to have ruby version 2.4.2 installed
- bundle install to install all gems
- start the server at port 3000
- Open http://localhost:3000/docs/index.html to view API docs
- Had installed pg to implement the persistance but ran out of time
